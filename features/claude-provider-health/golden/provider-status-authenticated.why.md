# WHY_CORRECT

`claude --version` succeeds and `claude auth status` returns JSON with `authenticated: true`, so the canonical health result must be `ready`, `available: true`, and `authStatus: "authenticated"` for provider `claudeCode`.
